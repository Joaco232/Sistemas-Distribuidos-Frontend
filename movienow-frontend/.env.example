# Backend API URL
# This is used by the frontend to make API calls
# When running locally (not in Docker): http://localhost:8080
# When running in Docker Compose: http://localhost:8080 (since browser runs on host)
VITE_API_BACK_URL=http://localhost:8080
